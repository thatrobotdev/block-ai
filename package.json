{
  "name": "block-ai",
  "description": "",
  "version": "0.0.2",
  "private": true,
  "repository": "thatrobotdev/block-ai",
  "license": "MIT",
  "scripts": {
    "build": "web-ext build --source-dir src/ --overwrite-dest",
    "lint": "web-ext lint --source-dir src/",
    "start": "web-ext run --source-dir src/ --firefox"
  },
  "devDependencies": {
    "web-ext": "^8.4.0"
  }
}
